// assim como na matematica a ordem de prioridade é: multiplicação, divisao, soma, subtracao
double a = 4 / 2 + 2;
Console.WriteLine(a);

// para definirmos a prioridade que queremos, colocamos o numero em parenteses
double b = 4 / (2 + 2);
Console.WriteLine(b);

// definindo 